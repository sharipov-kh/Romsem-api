{
  "name": "json-server-app",
  "version": "1.0.0",
  "description": "A simple JSON Server for testing purposes",
  "main": "index.js",
  "scripts": {
    "start": "node ./node_modules/json-server/bin/index.js --watch db.json --port $PORT",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "json-server": "^1.0.0-beta.1"
  }
}
